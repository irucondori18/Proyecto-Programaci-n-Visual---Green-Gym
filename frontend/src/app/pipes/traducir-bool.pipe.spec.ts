import { TraducirBoolPipe } from './traducir-bool.pipe';

describe('TraducirBoolPipe', () => {
  it('create an instance', () => {
    const pipe = new TraducirBoolPipe();
    expect(pipe).toBeTruthy();
  });
});
